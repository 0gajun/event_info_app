<% if @eventinfo.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-danger">
			<%= @eventinfo.errors.count %>個のエラーがあります。
		</div>
		<ul>
			<% @eventinfo.errors.full_messages.each do |msg| %>
				<li>* <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
